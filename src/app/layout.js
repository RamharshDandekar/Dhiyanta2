import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Dhiyanta - RFP Intelligence Platform",
  description: "AI-powered RFP response system. From Data to Discernment. Automate RFP analysis, product matching, and pricing strategy with multi-agent AI.",
  keywords: "RFP, AI, proposal, analysis, B2B, automation, Gemini",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
